{% extends 'base.html.twig' %}

{% block title %}
Jérémy Sam - Connection Controller
{% endblock %}

{% block body %}
    <div class="container mt-4">
    {% if(isConnected) %}
        La base de données "{{ dbName }}" est connectée
    {% else %}
        Aucune connexion avec la base de données "{{ dbName }}"
    {% endif %}
    </div>
{% endblock %}
